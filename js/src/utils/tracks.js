/**
 * External dependencies
 */
import { noop } from 'lodash';

export const recordSfwEvent = noop;
export const addBaseEventProperties = noop;
export const recordStepperChangeEvent = noop;
export const recordStepContinueEvent = noop;
